# Profil security-test: prod-gleiche Security-Chain für SecurityMatrixIT (TC-040).
# Wird nur via @ActiveProfiles("security-test") aktiviert.
#
# Bootstrap-Credentials bewusst NICHT überschrieben: die ITs teilen sich die DB,
# und der ORGANISATOR wird vom zuerst gestarteten Context angelegt — alle Profile
# müssen daher dieselben Dev-Defaults aus application.properties verwenden.
auth.jwt.secret=security-test-secret-mindestens-32-zeichen-lang!
